PI = 3.14159

radius = float(input('Enter the radius: '))
height = float(input('Enter the height: '))

surface_area = 2 * PI * radius * (radius + height)

print('The surface area of the cylinder is', surface_area,'sqaure inches')
